import React from 'react'
import { Navigation } from './Navigation'

export const Header = () => {
    
    return (
        <header>
            <span>
                React Workshop
            </span>
            <Navigation />
        </header>
    )
}